import 'package:flutter/widgets.dart';

class RandomCharacter extends StatelessWidget {

  const RandomCharacter({super.key});
  
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}